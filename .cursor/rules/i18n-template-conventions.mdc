---
description: 本项目 i18n 与 Smarty 模板约定（防 key/引号搞错）
alwaysApply: true
---

# i18n 与模板约定

## 语言包 key 格式（本项目）

- **目标格式**：`短模块_五位编号`，如 `common_03021`、`wap_00421`、`admin_user_company_00012`。
- **模块名最多 3 段**，例如 `admin_user_company` 已到上限；禁止 `admin_user_company_component_addjob_00001` 这种整页路径。
- **常用模块**：`common_`、`wap_`、`wap_com_`、`wap_user_`、`member_com_`、`admin_com_`、`admin_user_company_` 等。
- **全站通用**（多页共用）才用 `common_`。
- 改模板前：在 `data/lang/auto/zh_cn.php` **grep 确认 key 已存在**；语义查 `data/lang/auto/index.json`。
- `zh_cn.php` 与 `en_us.php` **key 必须一一对应**。

## 模板写法

```smarty
{yun:}t key='wap_00439'{/yun}
```

- HTML 属性：外层双引号包 Smarty 标签时常用 `placeholder="{yun:}t key='...'{/yun}"`。
- **JS 字符串**：外层用**双引号**，内层 key 用单引号，例如：
  `showConfirm("{yun:}t key='wap_00439'{/yun}", function(){`
- 禁止：`'...{yun:}t key='xxx'{/yun}...'`（内外单引号冲突）。

## 改动后

- 动过 `.htm` 后，删除或让 Smarty 重建 `data/templates_c/` 中对应编译文件。

## 禁止

- 使用整页路径当模块名（过长 key）。
- 未核对 key 格式就批量替换语言包或模板。
