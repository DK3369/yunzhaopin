---
description: H5 字号收口与公开 Nearby 入口；筛选项必须写 rem，不要继承 body 0.9em
globs: web/apps/site/**,web/layers/ui/**
alwaysApply: true
---

# H5 CSS 与公开 Nearby

改 PC/H5 前台只动 `web/`，收口写在 `web/apps/site/app/assets/main.css`，**不要**改 `public/legacy/h5/css/yunwap.css` / `base.css`。

## 根字号

H5 `html { font-size: clamp(32px, 10vw, 75px) }`。legacy `body { font-size: 0.9em }` 会把没写 rem 的字放大到三十多像素。可见正文必须自己写 rem，不要靠继承 `em`。

收口（390 宽约 14px / 12px）：

- 标题 `0.4rem`
- 正文 / 列表项 `0.373333rem`，行高 `0.56rem`
- 说明 / 分组标签 `0.32rem`，行高 `0.48rem`

## Country / Job function / Filter

`H5FilterBar` 点开后的列表（`.h5-filter-panel` / `.h5-filter-item`）必须带上述正文 rem。分组标签用说明尺寸。写在 `main.css` 的 `.site-h5` 下，不要给组件加 scoped 再写一套 px。`.job_header_nav` 固定 `height: 1.2rem` 会把面板裁掉，H5 应收成 `height: auto` + `flex-wrap`，面板单独一行、`z-index` 盖住职位卡。

顶栏筛选项（Country / Job function / Filter）yunwap 写死 `width: 1.1rem` + nowrap，英文会被截成 `Cou…`。Latest 不要占 50%。筛选项 `width: auto`，字号用正文 `0.346667rem`，给三角留 `padding-right`。

## Nearby 先藏

公开 `/map`（Nearby / Map Search）从 **PC 顶栏、H5 宫格、页脚、H5 `/jobs` 顶栏 tab** 拿掉。过滤用 `isPublicMapTo`（只认路径 `/map`，不要误伤 `/com/map`）。

**保留**：路由 `pages/map.vue`、职位/兼职/企业详情「看地图」、招聘会员 `/com/map`。不要删接口 `/v1/wap/map/*`。

H5 `/jobs` 顶栏只要 Latest，不要 Urgent Hiring，也不要 Nearby。

## 会员 H5 标签行

`.member_c9` 不要沿用绝对定位 + `.com_member_hr_p1` 的 `padding-left: 1.866667rem`（按中文四字留白）。英文 Inquiry Job / Company Reply 会叠到内容上。H5 改成 flex，标签 `position: static`。
