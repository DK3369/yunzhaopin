[package]
name = "phpyun-transport-mq"
version.workspace = true
edition.workspace = true

# The event-bus adapter: turns a queue message into a kernel `Ctx` plus a typed
# payload, and owns the concerns HTTP does not have — idempotency, retry
# backoff, and dead-lettering.
[dependencies]
phpyun_core.workspace = true
phpyun_kernel.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true
