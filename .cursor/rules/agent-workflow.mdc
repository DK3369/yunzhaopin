---
description: 禁止批量工具、多余文件、未理解上下文就改代码
alwaysApply: true
---

# 修改纪律

## 禁止使用任何批量工具修改

- **不得**运行或编写会对多文件/全目录做自动替换的脚本，包括但不限于：
  - `tools/i18n_htm_literals.php`、`fix_htm_js_t_quotes.php`、`migrate_auto_lang_keys.php` 等批量 i18n/引号/迁移脚本
  - `git checkout -- uploads/`、`find … | xargs sed` 等批量回滚或批量替换
- 模板、语言包、PHP 等改动：**逐文件、逐条**处理；改一个、验一个、提交一个（或一小批相关文件一次提交）。
- 用户明确要求批量处理时，须先说明影响范围与回滚方式，得到同意后再做。

## 禁止产生无用和多余的文件

- 不创建用户未要求的：报告、缓存 JSON、`.bak`、临时脚本、重复工具、说明文档等。
- 不提交调试产物、`data/templates_c/` 编译缓存、一次性扫描结果，除非用户明确要求保留。
- 新工具脚本仅在用户明确要求且无法用手动步骤完成时添加；优先改现有文件，不堆新文件。
- 删除自己引入的、已无用的临时文件，不要留在仓库里。

## 禁止未研究通上下文就修改代码

改任何文件前必须先弄清：

1. **调用链**：谁引用这段代码（模板、控制器、model、JS、语言包）。
2. **数据从哪来**：变量赋值、Smarty 标签、i18n key 格式（如 `wap_00703` vs 中文 key）。
3. **边界与约定**：引号嵌套、现有 `{yun:}t` / `yun_at()` 用法、项目里同类文件怎么写。
4. **影响面**：改一处会不会破坏编译缓存、别名映射、英文语言包对齐。

未读 surrounding code、未 grep 引用关系、未确认 key 格式时，**不得**动手改。

不确定时先向用户说明现状与方案，或只读排查，不先改。
