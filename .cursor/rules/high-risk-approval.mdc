---
description: 删改、回滚、大范围改动须用户明确同意
alwaysApply: true
---

# 高风险操作须先征得同意

以下操作**默认禁止**，除非用户当次对话里明确说了可以做：

## 须先说明并同意

- `git checkout` / `git restore` / `git reset` 覆盖**任意目录或多个文件**
- `git push --force`、`git reset --hard`
- 删除语言包、模板、`tools/` 下脚本、`data/lang/auto/aliases.php` 等
- 单次任务改动 **超过 3 个文件**（同一逻辑的小提交除外，仍须先列出清单）
- 运行会**写入**文件的 `tools/*.php` 扫描/迁移脚本

## 说明须包含

1. 要动哪些路径、大约多少文件  
2. 能否用 git 恢复、当前是否有未提交改动  
3. 更安全的替代方案（例如只改一个文件）

用户未回复同意前：**只读排查**，不执行。

## 出问题时

用户说「挂了」「数据没了」时：**先调查**（`git status`、`git log`、读文件、查备份），**不得**用整目录 checkout 当第一招。
