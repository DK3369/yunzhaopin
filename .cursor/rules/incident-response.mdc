---
description: 故障与数据丢失时先调查再动手
alwaysApply: true
---

# 故障响应：先查后改

用户反馈「挂了」「数据不显示」「翻译丢了」时：

## 第一步（只读）

1. `git status`、`git diff`、`git log -5` — 弄清未提交改动与最近提交  
2. 读用户点名的文件，对照 git 里上一版  
3. 查能否恢复：`git stash list`、Cursor Local History、`.bak`（勿假设 `.bak` 一定正确）  
4. 有 Web 环境则 `curl` 相关页面，看真实报错

## 第二步（汇报）

用中文说明：**丢了什么、还在什么、git 里有没有、能否恢复、建议怎么做**。  
找不到就说找不到，**不编造**「应该能恢复」。

## 第三步（修复）

- 优先**最小 diff**：单文件、可回滚的 commit  
- 禁止第一反应：`git checkout -- uploads/` 或整目录回滚  
- 每修一块就验证一块，按 `verify-before-done` 规则执行

## 禁止

- 未查清原因连续试多种「修复」（尤其是批量脚本）  
- 空洞道歉代替技术结论  
- 在用户确认前删除或覆盖大量文件
