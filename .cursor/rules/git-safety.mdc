---
description: Git 回滚禁令与每次改动后必须提交（中文说明）
alwaysApply: true
---

# Git 安全与提交规范

## 禁止危险回滚

1. **禁止回滚超过一天的内容**
   - 不得对超过 24 小时前的文件/目录执行 `git checkout`、`git restore`、`git reset` 等回退操作。
   - 若必须处理更早历史，先与用户确认，并说明影响范围。

2. **禁止未提交就回滚**
   - 执行任何回滚前，必须先确认工作区状态：
     - 有未提交改动时，**不得**直接 `git checkout -- <path>` 覆盖。
     - 应先 `git status` / `git diff`，必要时先 `git add` + `git commit` 或 `git stash`，再操作。
   - **严禁**在未备份、未提交的情况下批量回滚整个目录（如 `uploads/`）。

## 每次写完必须提交

完成一批有实质内容的修改后（同一任务收尾时），**必须**创建 git 提交：

1. 先执行 `git status`、`git diff`，确认只提交本次相关文件。
2. 用中文写清 commit message：改了什么、为什么改（1～2 句即可）。
3. 示例：

```
修复 wap/resume.htm 退出登录弹窗的 JS 引号冲突

将 showConfirm 外层改为双引号，避免与 {yun:}t key='...' 嵌套冲突。
```

4. 不得提交明显含密钥的文件（`.env`、凭证等）。
5. 用户未明确要求时，**不要** `git push`。

## 与 i18n / 批量脚本

- 禁止未经用户同意对全站做批量替换或批量回滚。
- 语言包、模板等改动优先小步提交，便于恢复。
