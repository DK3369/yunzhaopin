---
description: 改完必须验证，不能只说「应该好了」
alwaysApply: true
---

# 完成前必须验证

声称「已修复」「已恢复」之前，**必须自己跑过验证**，不能凭猜测收尾。

## 最低验证清单

| 改动类型 | 必须做 |
|----------|--------|
| PHP | `php -l` 语法检查 |
| 语言包 `data/lang/auto/*.php` | `php -l` + `include` 能加载 + key 格式与项目一致 |
| Smarty `.htm` | 确认引号未冲突；必要时清对应 `data/templates_c/` 编译缓存 |
| i18n 模板 | grep 该文件无残留硬编码中文（若本次任务就是 i18n） |

## 能测则测

- 有 Web 环境时：用实际域名/`curl` 访问相关页面，确认 HTTP 200、无 Fatal/Parse Error、列表数据能出来。
- 验证失败：**如实汇报**，继续修；不得换一批未验证的改动糊弄。

## 禁止

- 未验证就让用户「刷新试试」。
- 用「我这边测了」搪塞，实际没跑命令。
