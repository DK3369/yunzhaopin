---
description: H5 字号收口与公开 Nearby 入口；筛选项必须写 rem，不要继承 body 0.9em
globs: web/apps/site/**,web/layers/ui/**
alwaysApply: true
---

# H5 CSS 与公开 Nearby

改 PC/H5 前台只动 `web/`，收口写在 `web/apps/site/app/assets/main.css`，**不要**改 `public/legacy/h5/css/yunwap.css` / `base.css`。

## PC / H5 隔离（禁止交叉污染）

两端同一路由、同一 `main.css`，靠 class 显隐，不是两套源码。改样式必须带端前缀：

- **H5**：`.site-h5` 或 `@media (max-width: 1199px)`
- **PC**：`.site-pc` 或 `@media (min-width: 1200px)`

禁止改无前缀的共用 class（`.pager`、`.search_job_list`、yunwap 原类）除非确认两端都该变。改 H5 前在 1366 看一眼对应 PC 页；改 PC 前在 390 看一眼对应 H5 页。

PC `/jobs` 卡片 `float:left`。分页要用 `.search_pages`（或 `clear:both; width:100%`），不能让 `.pager` 的 flex 盒贴到第一张卡右侧。已申请钮不要写死 66px；More 下拉不要写死 100px、城市芯片不要写死 40px，英文会截成 `AlreadyA` / `Company Benefi` / `Unli…`。

PC 首页 `.pc-home-search` 的 Jobs 下拉必须 `z-index` 高于 `.leftNav`（900），否则会钻到左侧分类和 banner 下面。

## H5 切页

不要让切页把 legacy CSS 或站标卸掉：stylesheet 用稳定 `key`，H5 顶栏 `v-show`，公开 `NuxtPage` `:keepalive="{ max: 5 }"`。隐藏的 `.site-pc` 在 H5 下 `content-visibility: hidden`，避免跟着解码一大堆 PC 图。

## 根字号

H5 `html { font-size: clamp(32px, 10vw, 75px) }`。legacy `body { font-size: 0.9em }` 会把没写 rem 的字放大到三十多像素。可见正文必须自己写 rem，不要靠继承 `em`。

收口（390 宽约 14px / 12px）：

- 标题 `0.4rem`
- 正文 / 列表项 `0.373333rem`，行高 `0.56rem`
- 说明 / 分组标签 `0.32rem`，行高 `0.48rem`

## Country / Job function / Filter

`H5FilterBar` 点开后的列表（`.h5-filter-panel` / `.h5-filter-item`）必须带上述正文 rem。分组标签用说明尺寸。写在 `main.css` 的 `.site-h5` 下，不要给组件加 scoped 再写一套 px。`.job_header_nav` 固定 `height: 1.2rem` 会把面板裁掉，H5 应收成 `height: auto` + `flex-wrap`，面板单独一行、`z-index` 盖住职位卡。

顶栏筛选项（Country / Job function / Filter）yunwap 写死 `width: 1.1rem` + nowrap，英文会被截成 `Cou…`。Latest 不要占 50%。筛选项 `width: auto`，字号用正文 `0.346667rem`，给三角留 `padding-right`。

## Nearby 先藏

公开 `/map`（Nearby / Map Search）从 **PC 顶栏、H5 宫格、页脚、H5 `/jobs` 顶栏 tab** 拿掉。过滤用 `isPublicMapTo`（只认路径 `/map`，不要误伤 `/com/map`）。

**保留**：路由 `pages/map.vue`、职位/兼职/企业详情「看地图」、招聘会员 `/com/map`。不要删接口 `/v1/wap/map/*`。

H5 `/jobs` 顶栏只要 Latest，不要 Urgent Hiring，也不要 Nearby。

## 会员 H5 标签行

`.member_c9` 不要沿用绝对定位 + `.com_member_hr_p1` 的 `padding-left: 1.866667rem`（按中文四字留白）。英文 Inquiry Job / Company Reply 会叠到内容上。H5 用三列 grid：标签左、内容右对齐，删除钮在末列。不要改 `memberwap.css`。

`.m_tab` 不要沿用 `height: 2.826666rem`。两行状态/天数 tab 时卡片负 margin 会切掉第二行。H5 `height: auto`，只给最后一行 tab 留叠进蓝底的 padding。筛选项用说明字号、nowrap；天数用 `3 Days` / `7 Days` / `15 Days` / `30 Days`，不要 `Within one week` 和光秃的 `15`。职位卡标题 `.Posted_card_name` 最多 3 行。
