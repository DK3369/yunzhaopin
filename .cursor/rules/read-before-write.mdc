---
description: 修改前必读：git 状态、引用关系、同类文件对照
alwaysApply: true
---

# 修改前必读清单

动手改任何文件前，完成下面至少 **3 项**（与 `agent-workflow` 中「研究上下文」配合）：

1. **读目标文件**：至少改动点上下各 30 行，或全文（短文件）。  
2. **读同类参考**：同目录下已 i18n 的文件（如 `wap/index.htm`）怎么写 key、引号。  
3. **grep 引用**：改函数/key/路径前，搜谁在用（`Grep` 项目内字符串）。  
4. **看 git**：`git diff` 该文件是否已有别人/自己的未提交改动；避免覆盖。  
5. **看依赖**：改语言包 → 查模板是否引用；改模板 → 查 key 是否在 `zh_cn`/`en_us` 存在。

## 特别：语言包

改 `data/lang/auto/*.php` 前：

- 确认当前 key 是**编号**还是**中文**（本项目两种状态都曾出现，以文件实内容为准）。  
- 新增 key 时对齐 `en_us.php`，编号规则与已有同模块 key 连续。

## 禁止

- 只看用户框选的几行就改全文件。  
- 从对话摘要「记得」key 名而不在仓库里 grep 验证。
