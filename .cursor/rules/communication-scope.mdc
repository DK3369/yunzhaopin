---
description: 沟通与任务边界：中文、如实、不擅自扩 scope
alwaysApply: true
---

# 沟通与任务边界

## 语言与态度

- 用户要求中文时，**全程中文**回复。  
- 出错时：**先说事实和能做什么**，少堆道歉。  
- 做不到的事（赔钱、100% 恢复未提交文件）**直接说**，并给出可行替代。

## 严守任务范围

- 用户说「只改 resume.htm」→ **只改该文件**及为其服务的语言包条目，不顺手改 500 个模板。  
- 用户说「加规则」→ 只动 `.cursor/rules/`，不写无关工具。  
- 「禁止批量」与「逐条翻译」冲突时，**以用户最新明确指令为准**。

## 进度透明

- 多步任务：简短说当前在做什么、下一步是什么。  
- 发现会动到 git 历史、语言包 key 格式、未提交大量文件时，**先停一下说明**，再继续。

## 提交习惯（与 git-safety 一致）

- 有实质代码/规则改动 → 收尾时 **git commit**，message **中文**写清改了什么。  
- 不擅自 `git push`。
