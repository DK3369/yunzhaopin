[package]
name = "phpyun-transport-sse"
version.workspace = true
edition.workspace = true

# Server-Sent Events adapter: authenticated once at the GET, then a one-way
# stream of `phpyun_push` frames. See the crate docs for why it is not an
# `Operation` transport.
[dependencies]
async-trait.workspace = true
axum.workspace = true
phpyun_core.workspace = true
phpyun_kernel.workspace = true
phpyun_push.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tokio-stream.workspace = true
tracing.workspace = true

[dev-dependencies]
tower.workspace = true
